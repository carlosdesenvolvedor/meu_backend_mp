# Credenciais da Conta Principal (SJP)
MP_TOKEN_SJP="SEU_ACCESS_TOKEN_DA_CONTA_SJP"
MP_SECRET_SJP="SEU_WEBHOOK_SECRET_DA_CONTA_SJP"

# Credenciais da Conta Secundária (Amarela)
MP_TOKEN_AMARELA="SEU_ACCESS_TOKEN_DA_CONTA_AMARELA"
MP_SECRET_AMARELA="SEU_WEBHOOK_SECRET_DA_CONTA_AMARELA"

# Credenciais do Firebase (para o webhook atualizar o banco)
# Conteúdo do seu arquivo JSON de chave de serviço, codificado em Base64
# Comando para gerar no Linux/Mac: base64 -w 0 seu-arquivo-de-servico.json
# No Windows (PowerShell): [Convert]::ToBase64String([IO.File]::ReadAllBytes("seu-arquivo-de-servico.json"))
FIREBASE_SERVICE_ACCOUNT_BASE64="SUA_CHAVE_DE_SERVICO_EM_BASE64"

# Porta em que o servidor vai rodar
PORT=3000
